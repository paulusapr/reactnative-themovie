import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  header: {
    paddingTop: 10,
    paddingBottom: 30,
  },
  headerText: {
    fontSize: 20,
  },
  screen: {
    padding: 10,
    flex: 1,
  },
});

export default styles;
